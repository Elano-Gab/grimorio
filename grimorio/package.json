{
  "name": "grimorio",
  "version": "1.0.0",
  "private": true,
  "description": "Grimório — códice financeiro pessoal",
  "scripts": {
    "android": "npx cap add android && npx cap sync android",
    "apk": "cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^8.0.0",
    "@capacitor/core": "^8.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0"
  }
}
